require 'spec_helper'

describe Spinjs::For::Rails do
  it 'has a version number' do
    expect(Spinjs::For::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
